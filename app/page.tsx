import LoginPage from "@/components/login-page"

export default function Home() {
  // In a real app, check if user is authenticated and redirect to dashboard if true
  // For demo purposes, we'll just show the login page
  return <LoginPage />
}

